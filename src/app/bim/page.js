import State from "@/components/states";
import Form from "../form";

const Bim =()=>{
    return (
        <div>BIM it is !
            <State />
            <Form />
        </div>
    );
}

export default Bim